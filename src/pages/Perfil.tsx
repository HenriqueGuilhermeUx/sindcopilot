import { useAuth } from "@/_core/hooks/useAuth";import { trpc } from "@/lib/trpc";import { Button } from "@/components/ui/button";import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Badge } from "@/components/ui/badge";import { Checkbox } from "@/components/ui/checkbox";import { User,Shield,Crown,Save,Loader2,CheckCircle2 } from "lucide-react";import { toast } from "sonner";import { useState,useEffect } from "react";
const CONTACT_EMAIL="henriquecampos66@gmail.com",LGPD_VERSION="1.0";
export default function Perfil(){const{user}=useAuth();const profile=trpc.profile.get.useQuery(undefined,{enabled:!!user});const updateProfile=trpc.profile.update.useMutation();const acceptLgpd=trpc.profile.acceptLgpd.useMutation();const billingStatus=trpc.billing.status.useQuery(undefined,{enabled:!!user});const utils=trpc.useUtils();const[form,setForm]=useState({name:"",phone:"",cpf:"",creci:"",company:""});const[lgpdAccepted,setLgpdAccepted]=useState(false);const[termsAccepted,setTermsAccepted]=useState(false);useEffect(()=>{if(profile.data){setForm({name:profile.data.name||"",phone:profile.data.phone||"",cpf:profile.data.cpf||"",creci:profile.data.creci||"",company:profile.data.company||""});if(profile.data.lgpdConsentAt)setLgpdAccepted(true);if(profile.data.termsAcceptedAt)setTermsAccepted(true)}},[profile.data]);const save=async()=>{if(!lgpdAccepted||!termsAccepted)return toast.error("Aceite os Termos e a Política de Privacidade");try{if(!profile.data?.lgpdConsentAt)await acceptLgpd.mutateAsync({version:LGPD_VERSION});await updateProfile.mutateAsync(form);utils.profile.get.invalidate();toast.success("Perfil atualizado!")}catch(e:any){toast.error(e.message||"Erro ao atualizar perfil")}};const plan=billingStatus.data?.plan||"free";return <div className="space-y-6"><div><h1 className="text-2xl font-bold">Meu Perfil</h1><p className="text-muted-foreground">Gerencie seus dados e configurações.</p></div><Card><CardHeader className="pb-3"><div className="flex items-center justify-between"><CardTitle className="flex items-center gap-2"><Crown className="h-5 w-5 text-amber-500"/>Plano Atual</CardTitle><Badge>{plan}</Badge></div></CardHeader><CardContent className="flex justify-between text-sm"><span>{billingStatus.data?.maxCondominiums||1} condomínios | {billingStatus.data?.maxAssistants||0} ajudantes</span><Button variant="outline" size="sm" asChild><a href="/planos">Alterar Plano</a></Button></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><User className="h-5 w-5 text-blue-600"/>Dados Pessoais</CardTitle><CardDescription>Informações do síndico profissional</CardDescription></CardHeader><CardContent><div className="grid md:grid-cols-2 gap-4">{[["name","Nome Completo",form.name],["phone","Telefone / WhatsApp",form.phone],["cpf","CPF",form.cpf],["creci","CRECI / Registro",form.creci],["company","Empresa / Razão Social",form.company]].map(([key,label,value])=><div className="space-y-2" key={key}><Label htmlFor={key}>{label}</Label><Input id={key} value={value} onChange={e=>setForm(f=>({...f,[key]:e.target.value}))}/></div>)}<div className="space-y-2"><Label>Email</Label><Input value={profile.data?.email||""} disabled/></div></div></CardContent></Card><Card><CardHeader><CardTitle className="flex items-center gap-2"><Shield className="h-5 w-5 text-emerald-600"/>Privacidade e Consentimento</CardTitle><CardDescription>Em conformidade com a LGPD</CardDescription></CardHeader><CardContent>{profile.data?.lgpdConsentAt?<div className="flex items-center gap-2 text-sm text-emerald-700 bg-emerald-50 p-3 rounded-lg"><CheckCircle2 className="h-4 w-4"/>Consentimento registrado em {new Date(profile.data.lgpdConsentAt).toLocaleDateString("pt-BR")}</div>:<div className="space-y-3"><p className="text-sm text-muted-foreground">Seus dados são tratados para prestar o serviço. Direitos podem ser solicitados em {CONTACT_EMAIL}.</p><label className="flex gap-3 text-sm"><Checkbox checked={lgpdAccepted} onCheckedChange={c=>setLgpdAccepted(!!c)}/>Aceito a Política de Privacidade.</label><label className="flex gap-3 text-sm"><Checkbox checked={termsAccepted} onCheckedChange={c=>setTermsAccepted(!!c)}/>Aceito os Termos de Uso.</label></div>}</CardContent></Card><div className="flex justify-end"><Button onClick={save} disabled={updateProfile.isPending||acceptLgpd.isPending}>{updateProfile.isPending?<Loader2 className="h-4 w-4 animate-spin mr-2"/>:<Save className="h-4 w-4 mr-2"/>}Salvar Perfil</Button></div></div>}
